# Функције

## Шта су функције?

* Потпрограми
* Делови кода који могу да се рециклирају

## Начини писања функција

1. Класично:

```js
function ime(par1,par2,...){
    <telo fje>
    return <povratna vrednost>
}
```

2. Функцијски изрази:

```js
let ime = function(par1,par2,...){
    <telo fje>
    return <povratna vrednost>
}
```

3. Arrow функције

```js
let ime = (par1,par2,...) => {
    <telo fje>
    return <povratna vrednost>
}
```

## Зашто користимо функције?

1. Рециклирање кода
2. Апстракција
    - isEven(num) је читкије од num % 2 == 0

## Предности Arrow функција:

1. Кратак запис, погодне за прослеђивање другим функцијама (forEach,sort,filter,map,reduce,...)
2. Не дефинишу свој "this", за разлику од класичних функција

--------------------------------------------

## Задаци

1. Написати функцију која исписује да ли је број дељив са 3

```js
function printIsDivisible3(num){
    if(num % 3 == 0){
        console.log(`Број ${num} је дељив са 3`)
    }
    else{
        console.log(`Број ${num} није дељив са 3`)
    }
}
```

2. Написати функцију која проверава да ли је број дељив са 3

```js
function isDivisible3(num){
    return num % 3 == 0 // враћа Булову вредност (true/false)
}
```

3. Написати функцију која враћа суму бројева од Н до М

```js
function sumRange(n,m){
    ...
}
```

4. Написати функцију која враћа производ бројева од Н до М

```js
function productRange(n,m){
    ...
}
```

5. Написати функцију која проверава да ли је троугао валидан (на основу унете три странице). (Троугао је валидан ако су све странице позитивне и збир сваке две странице је већи од треће)

```js
function isValid(a,b,c){
    ...
}
```

6. Написати функцију која проверава да ли је унет стринг палиндром

```js
function isPalindrome(str){
    ...
}
```

7. Написати функцију која прима низ људи који су лајковали неки пост, и онда враћа String у следећем формату:

```js
[]  => "Никоме се не свиђа"

["Петар"] => "Петар воли ово"

["Јаков", "Алекса"] => "Јаков и Алекса воле ово"

["Максимилијан", "Џони", "Марко"] => "Максимилијан,Џони и Марко воле ово"

["Алекса", "Јаков", "Марко", "Максимилијан"] => "Алекса,Јаков и 2 других воле ово"

["Алекса", "Јаков", "Марко", "Максимилијан","Габријела"] => "Алекса,Јаков и 3 других воле ово"
```

8. Деда Мраз има низ са именима деце која су учинила добра дела у претходној години,
Помозите му да подели поклоне деци која то заслужују
    * Деца заслужују поклоне ако се налазе 2 или више пута у том низу

    Написати функцију која прима Деда Мразов низ, и враћа низ који садржи само децу која заслужују поклоне

```js
['pera','ana','pera','zika','lana','ana'] => ['pera','ana']
```
```js
function nonNaughtyList(arr){
    ...
}
``` 


